name: Recon (temporary)

on:
  push:
    paths:
      - 'recon-request/**'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  recon:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run recon script
        run: bash recon-request/recon.sh
